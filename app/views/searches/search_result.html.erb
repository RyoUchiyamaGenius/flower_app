<h2>検索結果</h2>

<div class="column">
    <ul class="items">
        <% @flowers.each do |flower| %>
            <li><%= link_to flower.name, "/searches/#{flower.id}" %></li>
        <% end %>
    </ul>
</div>